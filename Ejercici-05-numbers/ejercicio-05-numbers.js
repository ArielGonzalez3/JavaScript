let altura_cm = 174;
let altura_m = 1.74;
let peso_kg = 52.4;

let altura_m_redon = altura_m.toFixed();
console.log(altura_m_redon);

let peso_kg_redon = peso_kg.toPrecision(2);
console.log(peso_kg_redon);

let max_valor_js = Number.MAX_VALUE + 1;
console.log(max_valor_js);
